# Mobile Shop — প্রজেক্ট ওভারভিউ

> এই ডকুমেন্টটি প্রজেক্টটি অন্য কাউকে (শিক্ষক, টিমমেট, ইন্টারভিউয়ার) বুঝানোর জন্য লেখা হয়েছে।

---

## এক নজরে

**Mobile Shop** একটি ওয়েব ভিত্তিক **মোবাইল ফোন ই-কমার্স ফ্রন্টএন্ড** অ্যাপ্লিকেশন। ব্যবহারকারী বিভিন্ন ব্র্যান্ডের স্মার্টফোন দেখতে পারে, প্রোডাক্টের বিস্তারিত দেখতে পারে, পছন্দের ফোন **ফেভারিট** ও **কার্ট**-এ রাখতে পারে এবং **অর্ডার/চেকআউট** প্রক্রিয়া সম্পন্ন করতে পারে (UI স্তরে)।

| বিষয় | বিবরণ |
|--------|--------|
| **প্রজেক্টের ধরন** | Single Page Application (SPA-style) ওয়েব অ্যাপ |
| **ফ্রেমওয়ার্ক** | Next.js 16 (App Router) |
| **ভাষা** | TypeScript |
| **স্টাইলিং** | Tailwind CSS v4 |
| **ডেটা** | বাহিরের REST API (Programming Hero Open API) |
| **ব্যাকএন্ড** | নেই — বর্তমানে শুধু ফ্রন্টএন্ড |

---

## প্রজেক্টের উদ্দেশ্য

1. আধুনিক ওয়েব টেকনোলজি (React, Next.js) দিয়ে একটি **ই-কমার্স টাইপের UI** তৈরি করা  
2. **API থেকে ডেটা fetch** করে ডাইনামিক প্রোডাক্ট লিস্ট দেখানো  
3. **কার্ট, ফেভারিট, ফিল্টার, ডিটেইল পেজ** — রিয়েল শপের মতো ব্যবহারকারী অভিজ্ঞতা দেওয়া  
4. শেখার ও পোর্টফোলিওর জন্য একটি **সম্পূর্ণ ফ্রন্টএন্ড প্রজেক্ট** তৈরি করা  

---

## মূল ফিচারসমূহ

### হোমপেজ (`/`)

- **হিরো সেকশন:** ফিচার্ড ফোন, ক্যারোসেল, ৩D মডেল ভিউয়ার (Google Model Viewer)
- **প্রোডাক্ট গ্রিড:** API থেকে লোড হওয়া ফোন কার্ড (ছবি, নাম, ব্র্যান্ড, দাম)
- **ব্র্যান্ড ফিল্টার:** Apple, Samsung, Xiaomi ইত্যাদি দিয়ে ফিল্টার
- **ফেভারিট:** হার্ট আইকন দিয়ে পছন্দের ফোন সেভ (ব্রাউজারে `localStorage`)
- **শপিং কার্ট:** আইটেম যোগ/বিয়োগ, পরিমাণ বদল, সাবটোটাল + ডেলিভারি চার্জ
- **Buy Now:** দুই ধাপের মডাল — পরিমাণ নির্বাচন → অর্ডার সফল মেসেজ
- **Load More:** আরও প্রোডাক্ট লোড করা

### প্রোডাক্ট ডিটেইল পেজ (`/food/[slug]`)

> নোট: URL-এ `food` নামটি আগের টেমপ্লেট থেকে রয়ে গেছে; আসলে এটি ফোন ডিটেইল পেজ।

- বড় প্রোডাক্ট ইমেজ ও থাম্বনেইল
- কালার, স্টোরেজ, রিজিয়ন সিলেকশন (UI)
- রেটিং, Pros/Cons, টেকনিক্যাল স্পেসিফিকেশন
- Shop Now, Add to Cart, EMI ও WhatsApp বাটন (UI)
- ডেলিভারি টাইমলাইন দেখানো

### ডেটা সংরক্ষণ (ক্লায়েন্ট সাইড)

| কী | কোথায় | কী সেভ হয় |
|-----|--------|-------------|
| `cart` | localStorage | কার্টের ফোন ও পরিমাণ |
| `favorites` | localStorage | পছন্দের ফোনের slug তালিকা |
| `selectedPhone` | sessionStorage | ডিটেইল পেজে যাওয়ার সময় সাময়িক ডেটা |

---

## টেকনিক্যাল আর্কিটেকচার

```
┌─────────────────────────────────────────────────────────┐
│                    ব্যবহারকারী (Browser)                  │
└───────────────────────────┬─────────────────────────────┘
                            │
┌───────────────────────────▼─────────────────────────────┐
│              Next.js Frontend (React 19)                 │
│  ┌─────────────┐  ┌─────────────┐  ┌──────────────────┐  │
│  │  Home Page  │  │ Detail Page │  │  localStorage    │  │
│  │  page.tsx   │  │ food/[id]   │  │  sessionStorage  │  │
│  └──────┬──────┘  └──────┬──────┘  └──────────────────┘  │
└─────────┼────────────────┼────────────────────────────┘
          │                │
          │    HTTPS fetch   │
          ▼                ▼
┌─────────────────────────────────────────────────────────┐
│     External API: openapi.programming-hero.com          │
│     GET /api/phones?search={keyword}                    │
└─────────────────────────────────────────────────────────┘
```

### API থেকে যা আসে

```json
{
  "status": true,
  "data": [
    {
      "brand": "Apple",
      "phone_name": "iPhone 15 Pro",
      "slug": "iphone-15-pro",
      "image": "https://..."
    }
  ]
}
```

### অ্যাপ্লিকেশনে যা গণনা/তৈরি হয় (API-তে নেই)

- **দাম** — প্রতিটি ফোনের `slug` থেকে স্থিরভাবে গণনা (৳১৫,০০০ – ৳১,৫০,০০০)
- **ডেলিভারি চার্জ** — অর্ডার মোটের ৫% (ন্যূন ৳৩০, সর্বোচ্চ ৳১০০)
- **রেটিং ও স্পেসিফিকেশন** — ডিটেইল পেজে ডেমো/হার্ডকোডেড ডেটা

---

## প্রযুক্তি স্ট্যাক

| স্তর | প্রযুক্তি |
|------|-----------|
| Framework | Next.js 16.1.1 |
| UI Library | React 19 |
| Language | TypeScript 5 |
| Styling | Tailwind CSS 4 |
| Fonts | Geist (next/font) |
| 3D | Google Model Viewer |
| Dev Server | Webpack mode (`next dev --webpack`) |

---

## প্রজেক্ট স্ট্রাকচার

```
MobileShop-Client/
├── app/
│   ├── layout.tsx           # গ্লোবাল লেআউট, ফন্ট, 3D script
│   ├── page.tsx             # হোমপেজ (মূল ফিচার)
│   ├── globals.css          # গ্লোবাল স্টাইল ও অ্যানিমেশন
│   └── food/[id]/page.tsx   # প্রোডাক্ট ডিটেইল পেজ
├── public/                  # স্ট্যাটিক ফাইল
├── next.config.ts
├── package.json
└── PROJECT_OVERVIEW.md      # এই ফাইল
```

---

## ইউজার ফ্লো (সহজ ভাষায়)

1. ব্যবহারকারী সাইট খোলে → API থেকে ফোন লোড হয়  
2. হিরোতে ফিচার্ড ফোন দেখে, নিচে গ্রিডে সব ফোন ব্রাউজ করে  
3. ব্র্যান্ড ফিল্টার বা ফেভারিট ব্যবহার করতে পারে  
4. একটি ফোনে ক্লিক করলে **ডিটেইল পেজ**-এ যায়  
5. **Add to Cart** বা **Buy Now** দিয়ে কেনাকাটা শুরু করে  
6. চেকআউটে সাবটোটাল + ডেলিভারি দেখে → **Order Successful** (ডেমো — সার্ভারে সেভ হয় না)

---

## বর্তমান সীমাবদ্ধতা (সৎভাবে বলা যায়)

নিচের বিষয়গুলো জানালে প্রজেক্ট সম্পর্কে বিশ্বাসযোগ্য ধারণা পাওয়া যায়:

1. **ব্যাকএন্ড নেই** — অর্ডার ডাটাবেসে যায় না  
2. **লগইন/পেমেন্ট নেই** — bKash, কার্ড ইত্যাদি সংযুক্ত নয়  
3. **দাম API থেকে নয়** — ডেমোর জন্য কোডে গণনা  
4. **কিছু UI অসম্পূর্ণ** — সার্চ বক্সের লজিক আছে কিন্তু ইনপুট নেই; WhatsApp/EMI বাটন লিংক ছাড়া  
5. **এক ফাইলে অনেক কোড** — `page.tsx` বড়; ভবিষ্যতে কম্পোনেন্টে ভাগ করার পরিকল্পনা যোগ করা যায়  

---

## ভবিষ্যৎ উন্নয়ন (রোডম্যাপ ধারণা)

প্রফেশনাল প্রোডাকশন সাইটের দিকে এগোতে যা যোগ করা যেতে পারে:

- [ ] ব্যাকএন্ড API + ডাটাবেস (অর্ডার, ইউজার)  
- [ ] লগইন / সাইনআপ  
- [ ] রিয়েল পেমেন্ট গেটওয়ে  
- [ ] অ্যাডমিন প্যানেল (প্রোডাক্ট ম্যানেজ)  
- [ ] সার্চ বার UI, SEO, Footer  
- [ ] কোড রিফ্যাক্টরিং (ছোট কম্পোনেন্ট)  
- [ ] ডিপ্লয় (Vercel / Netlify)  

---

## চালানোর নিয়ম

```bash
# ডিপেন্ডেন্সি ইনস্টল
npm install

# ডেভেলপমেন্ট সার্ভার
npm run dev

# ব্রাউজারে খুলুন
http://localhost:3000
```

**প্রোডাকশন বিল্ড:**

```bash
npm run build
npm start
```

---

## সংক্ষিপ্ত উপস্থাপনা (৩০ সেকেন্ডে বলার জন্য)

> *"আমি Next.js ও TypeScript দিয়ে একটি Mobile Shop ই-কমার্স ফ্রন্টএন্ড বানিয়েছি। Programming Hero-র Open API থেকে ফোনের তালিকা আনি, গ্রিড ও ডিটেইল পেজে দেখাই। কার্ট ও ফেভারিট localStorage-এ রাখি। ব্র্যান্ড ফিল্টার, ৩D হিরো সেকশন, অর্ডার মডাল আছে। এখনো ব্যাকএন্ড ও পেমেন্ট নেই — এটা ফ্রন্টএন্ড ও UX ফোকাসড প্রজেক্ট; পরে API ও ডাটাবেস যোগ করার পরিকল্পনা করা যায়।"*

---

## এক লাইনে (রেজুমে / GitHub description)

**Mobile Shop** — Next.js 16 + TypeScript ই-কমার্স UI; API-driven phone catalog, cart, favorites, and checkout flow (frontend demo).

---

*শেষ আপডেট: মে ২০২৬*
